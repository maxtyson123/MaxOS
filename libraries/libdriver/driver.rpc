package driver;
version 1.0;
path /server/
class <driver.h>
namespace LibDriver;

service class Driver driver_{id} {
    rpc activate;
    rpc deactivate;
    rpc initialise;
    rpc reset;

    rpc vendor_name;
    rpc device_name;
}